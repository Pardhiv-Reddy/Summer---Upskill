# FEATURE ENGINEERING: INTERACTION FEATURES, BINNING & AGGREGATION

## Concepts Learned
- Why binning is useful in machine learning
- How interaction features help derive hidden relationships between features
- How aggregation features capture group-level statistical information

## Important Methods / Functions
- `PolynomialFeatures()`
- `pd.cut()`
- `df.groupby().transform()`
- `agg()`

## Real-World Understanding
- Binning helps convert continuous values into meaningful ranges/categories
- Interaction features help expose relationships between multiple features
- Aggregation features help capture statistical patterns across groups in the data
